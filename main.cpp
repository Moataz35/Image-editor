// Problem1: This program apply the filters many times on the same image
// Create array of flags indicates which filter is applied to this photo
// Note: After we doing this edit the Black and White filter that uses another filter before it
// Problem2: When we convert an image with less than 3 channels to another one with 3 or more we didn't do that correctly
// Edit add_frame() to add a colorful frame to a grey image
#include <iostream>
#include "Image.h"

using namespace std;

int main() {
	
	return 0;
}